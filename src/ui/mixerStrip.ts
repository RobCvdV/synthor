/** Shared mixer-strip metrics so channel and instrument strips line up. */
export const STRIP_GAP = 4
/** Height of the row under the fader (instrument routing select). */
export const STRIP_FOOTER_HEIGHT = 18
